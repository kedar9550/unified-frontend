const fs = require('fs'); const lines = fs.readFileSync('d:/Ganesha/Varahiamma/Unified/unified-frontend/src/pages/serviceDesk/FeedbackOverview.jsx', 'utf8').split('\n'); let balance = 0; lines.forEach((line, i) => { const opens = (line.match(/<Box/g) || []).length; const closes = (line.match(/<\/Box>/g) || []).length; balance += opens - closes; if (opens !== closes) console.log('Line ' + (i+1) + ': open ' + opens + ', close ' + closes + ', balance ' + balance); });
